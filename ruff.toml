line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "B", "UP", "RUF"]
ignore = ["E501"]  # comprimento de linha já é tratado pelo formatador

[lint.per-file-ignores]
"tests/*" = ["B011"]
